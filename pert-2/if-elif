#include <stdio.h>

int main() {
    int nilai = 85;
    if (nilai >= 90) {
        printf("Grade A\n");
    } else if (nilai >= 80) {
        printf("Grade B\n");
    } else if (nilai >= 70) {
        printf("Grade C\n");
    } else {
        printf("Grade D\n");
    }
    return 0;
}
